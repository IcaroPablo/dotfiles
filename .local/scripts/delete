#!/bin/sh
# delete <path...> — confirm, then rm -rf the given paths.

set -f
clear 2>/dev/null

printf '\n\t%s' "$@"
printf '\n\ndelete? [y/N] '
read -r ans || exit 0

case "$ans" in
    [yY]) rm -rf -- "$@" ;;
esac
