#!/bin/sh

name="$1"
[ -z "$name" ] && name="$(fzf --print-query --prompt='name (dir ends with /): ' </dev/null)"
[ -z "$name" ] && exit 0

case "$name" in
    */) mkdir -p -- "$name" ;;
    *)  touch -- "$name" ;;
esac
