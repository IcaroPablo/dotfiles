#!/bin/sh
# shredtrash — empty the trash folder (~/.trash) after confirmation.

clear 2>/dev/null

printf '\nempty ~/.trash? [y/N] '
read -r ans || exit 0

case "$ans" in
    [yY]) rm -rf -- "$HOME/.trash" && mkdir -p -- "$HOME/.trash" ;;
esac
