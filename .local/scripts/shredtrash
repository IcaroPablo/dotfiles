#!/bin/sh

clear 2>/dev/null

TRASH="${DOT_TRASH:-$HOME/.trash}"

printf '\nempty %s? [y/N] ' "$TRASH"
read -r ans || exit 0

case "$ans" in
    [yY]) rm -rf -- "$TRASH" && mkdir -p -- "$TRASH" ;;
esac
